

spring:
  datasource:
    url:    jdbc:mysql://localhost:3306/42gg_end?serverTimezone=UTC&characterEncoding=UTF-8
    username: root
    password: /*password*/
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        default_batch_fetch_size: 100

logging-level:
  org.hibernate.SQL: debug