

spring:
  datasource:
    url:    jdbc:mysql://localhost:3306/my_db?serverTimezone=UTC&characterEncoding=UTF-8
    username: root
    password: wken7373
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        default_batch_fetch_size: 100

logging-level:
  org.hibernate.SQL: debug